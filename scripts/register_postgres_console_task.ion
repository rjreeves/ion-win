# Run this from the ion-win repository root. The task uses whichever ion-win.exe
# is resolved from PATH when it runs instead of a machine-specific debug build.
task create --force postgres-console -- ion-win.exe examples/postgres_console.ion
